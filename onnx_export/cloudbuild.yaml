steps:
  - name: 'python:3.10'
    entrypoint: 'bash'
    args:
      - '-c'
      - |
        pip install -q optimum[exporters] transformers torch accelerate && \
        python /workspace/onnx_export/convert_vits.py
    volumes:
      - name: 'workspace'
        path: '/workspace'
    timeout: '1800s'

timeout: '1800s'

options:
  machineType: 'E2_HIGHCPU_8'
  logging: CLOUD_LOGGING_ONLY
