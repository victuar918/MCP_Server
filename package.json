{
  "name": "asterion-mcp",
  "version": "2.0.0",
  "description": "ASTERION MCP Server — VedAstro L0 도구 + L1~L3 확장 구조 | SSE + Streamable HTTP 이중 transport",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "dev":   "node --watch index.js"
  },
  "dependencies": {
    "express":                      "^4.19.2",
    "cors":                         "^2.8.5",
    "@modelcontextprotocol/sdk":    "^1.10.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
